// src/utils/quoteUtils.ts
import { Product, CostView } from './insuranceTypes';

export function handleQuoteRequest(activeProducts, totalPremium, costView) {
  console.log('Quote requested', { activeProducts, totalPremium, costView });
  // Add your logic for handling the quote request
}